# API

Import cellfish as:

```python
import cellfish as cf
```

Layer-1 helpers live in `cf.io`, `cf.data`, `cf.pl`, and `cf.stats`.
Algorithm-specific code is lazy-loaded from `cf.ext.<tool>`.

```{toctree}
:maxdepth: 2

io
data
plot
stats
ext
```
